pluginGroup=de.suprexdev
pluginVersion=1.1.0

# Target IDE: IntelliJ IDEA (unified distribution since 2025.3).
# platformVersion must be a published build.
platformVersion=2026.1.3

# Compatibility lower bound (build number). 261 = 2026.1.
# No upper bound is set on purpose (see build.gradle.kts ideaVersion) so the plugin stays
# compatible with future IDEs; add `pluginUntilBuild` + wire it back if you ever need to cap it.
pluginSinceBuild=261

org.gradle.jvmargs=-Xmx2g -XX:+UseParallelGC
org.gradle.caching=true
# The IntelliJ Platform already ships the Kotlin stdlib, so don't bundle our own.
kotlin.stdlib.default.dependency=false
